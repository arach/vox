{
  "name": "@voxd/cli",
  "version": "0.4.0",
  "type": "module",
  "description": "CLI for the Vox local transcription runtime.",
  "bin": {
    "vox": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arach/vox.git",
    "directory": "packages/cli"
  },
  "dependencies": {
    "@voxd/sdk": "0.4.0"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --format esm"
  }
}
